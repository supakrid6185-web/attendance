# ระบบเช็คชื่อเข้าชั้นเรียน

ระบบเช็คชื่อแบบ Web Application สำหรับครูและนักเรียน ใช้งานผ่าน Browser ไม่ต้องติดตั้ง

## 🎯 ฟีเจอร์

### สำหรับนักเรียน (student.html)
- ✅ ลงทะเบียนนักเรียน (รหัส, ชื่อ, นามสกุล, อีเมล, เบอร์โทร, ชั้นเรียน)
- ✅ เช็คชื่อเข้าชั้นเรียน (เลือกบทเรียน + กรอกรหัส)
- ✅ ป้องกันเช็คชื่อซ้ำในวันเดียวกัน
- ✅ ป้องกันรหัสนักเรียนซ้ำ

### สำหรับครู (teacher.html)
- ✅ ดูรายการเช็คชื่อทั้งหมด (กรองตามวันที่/บทเรียน)
- ✅ ดูรายชื่อนักเรียนทั้งหมด
- ✅ แก้ไขข้อมูลนักเรียน (ทุกอย่างยกเว้นรหัส)
- ✅ ลบนักเรียน (พร้อม cascade delete ประวัติเช็คชื่อ)
- ✅ Export ข้อมูลเป็น CSV (เช็คชื่อ + นักเรียน)
- ✅ สร้าง QR Code สำหรับนักเรียน (auto-detect IP)
- ✅ ล้างข้อมูลทั้งหมด

## 🚀 วิธีใช้งาน

### 1. เปิด Live Server
```bash
# ใน VS Code
คลิกขวาที่ teacher.html → Open with Live Server
```

### 2. เข้าผ่าน LAN IP (แนะนำ)
```
http://192.168.x.x:5500/teacher.html
```

### 3. สร้าง QR Code
1. เปิดหน้าครู → กด "📱 QR Code"
2. ดาวน์โหลด QR Code
3. ให้นักเรียนสแกน

### 4. นักเรียนใช้งาน
1. สแกน QR Code → เปิด student.html
2. ลงทะเบียน (ครั้งแรก)
3. เช็คชื่อ (ครั้งต่อไป)

## 📁 โครงสร้างไฟล์

```
├── student.html          # หน้านักเรียน
├── teacher.html          # หน้าครู
├── student.js            # Logic นักเรียน
├── teacher.js            # Logic ครู
├── common.js             # ฟังก์ชันร่วมกัน
├── style.css             # Styles
├── setup-lan.md          # คู่มือตั้งค่า LAN
├── CHANGELOG.md          # ประวัติการเปลี่ยนแปลง
└── README.md             # ไฟล์นี้
```

## ⚙️ การตั้งค่า

### เพิ่ม/แก้ไขชั้นเรียน
แก้ไขไฟล์ `common.js`:
```javascript
const classes = [
    { value: 'ม.1/1', label: 'ม.1/1' },
    { value: 'ม.1/2', label: 'ม.1/2' },
    // เพิ่มชั้นเรียนที่นี่
];
```

### เพิ่ม/แก้ไขบทเรียน
แก้ไขไฟล์ `common.js`:
```javascript
const lessons = {
    'lesson1': 'บทที่ 1: HTML พื้นฐาน',
    'lesson2': 'บทที่ 2: CSS Styling',
    // เพิ่มบทเรียนที่นี่
};
```

## 🔒 ความปลอดภัย

- ✅ XSS Protection (escapeHtml)
- ✅ Input Validation
- ✅ Confirmation ก่อนลบข้อมูล
- ✅ localStorage (ข้อมูลอยู่ใน Browser)

## 💾 การสำรองข้อมูล

**วิธีสำรองข้อมูล:**
1. เปิดหน้าครู
2. กด "📥 Export CSV" (เช็คชื่อ)
3. กด "📥 Export นักเรียน" (รายชื่อนักเรียน)
4. เก็บไฟล์ CSV ไว้ใน Google Drive

**หมายเหตุ:** ข้อมูลเก็บใน localStorage ของ Browser ถ้าล้าง cache ข้อมูลจะหาย

## 📱 Responsive Design

- ✅ ใช้งานได้ทั้ง Desktop และ Mobile
- ✅ Modal ปรับขนาดตามหน้าจอ
- ✅ ตารางแสดงผลได้ดีบนมือถือ

## 🐛 Troubleshooting

### ปัญหา: นักเรียนเข้าไม่ได้จากมือถือ
**แก้ไข:** 
- ตรวจสอบว่าเชื่อมต่อ WiFi เดียวกัน
- ใช้ LAN IP แทน localhost (http://192.168.x.x:5500)

### ปัญหา: ข้อมูลหาย
**แก้ไข:**
- ข้อมูลเก็บใน localStorage
- ถ้าล้าง Browser cache ข้อมูลจะหาย
- Export CSV เป็นประจำเพื่อสำรองข้อมูล

### ปัญหา: QR Code ไม่ทำงาน
**แก้ไข:**
- ตรวจสอบ URL ใน QR Code ว่าถูกต้อง
- ลองเปิด URL ด้วยมือก่อน (พิมพ์ใน Browser)

## 📝 License

MIT License - ใช้งานได้ฟรี

## 👨‍💻 Developer

พัฒนาโดย AI Assistant (Kiro)
