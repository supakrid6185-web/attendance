# 🚀 คู่มือการตั้งค่า Supabase

## ขั้นตอนที่ 1: สร้าง Supabase Project

1. ไปที่ https://supabase.com
2. คลิก **"Start your project"**
3. Sign up ด้วย GitHub (ฟรี)
4. คลิก **"New project"**
5. กรอกข้อมูล:
   - **Name:** `attendance-system`
   - **Database Password:** (จดไว้ให้ดี!)
   - **Region:** Southeast Asia (Singapore)
6. คลิก **"Create new project"**
7. รอ 2-3 นาที จนสถานะเป็น "Active"

---

## ขั้นตอนที่ 2: สร้างตาราง

1. ไปที่ **"SQL Editor"** (เมนูซ้าย)
2. คลิก **"New query"**
3. เปิดไฟล์ `supabase-schema.sql`
4. Copy ทั้งหมดแล้ว Paste ใน SQL Editor
5. คลิก **"Run"** (หรือกด Ctrl+Enter)
6. ถ้าสำเร็จจะขึ้น "Success. No rows returned"

---

## ขั้นตอนที่ 3: คัดลอก API Keys

1. ไปที่ **"Settings"** → **"API"** (เมนูซ้าย)
2. คัดลอกข้อมูลเหล่านี้:

### **Project URL:**
```
https://xxxxxxxxxxxxx.supabase.co
```

### **anon public key:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6...
```

---

## ขั้นตอนที่ 4: ตั้งค่าในโค้ด

1. เปิดไฟล์ `supabase-config.js`
2. แก้ไขบรรทัดเหล่านี้:

```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';  // ← แทนที่ด้วย Project URL
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';  // ← แทนที่ด้วย anon public key
```

### **ตัวอย่าง:**
```javascript
const SUPABASE_URL = 'https://abcdefgh.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

3. บันทึกไฟล์

---

## ขั้นตอนที่ 5: ทดสอบ

1. เปิด Live Server
2. เปิด `http://192.168.0.155:5500/student.html`
3. ลองลงทะเบียนนักเรียน
4. เปิด `http://192.168.0.155:5500/teacher.html`
5. ตรวจสอบว่าเห็นข้อมูลนักเรียนหรือไม่

---

## ✅ ตรวจสอบว่าสำเร็จ

### **ใน Supabase Dashboard:**
1. ไปที่ **"Table Editor"**
2. เลือกตาราง **"students"**
3. ควรเห็นข้อมูลนักเรียนที่ลงทะเบียน

### **ใน Browser Console:**
1. กด F12 เปิด Developer Tools
2. ไปที่ Tab "Console"
3. ควรเห็น: `✅ Supabase Client initialized`
4. ไม่ควรมี Error สีแดง

---

## 🐛 แก้ปัญหา

### **ปัญหา: แสดง "กรุณาตั้งค่า Supabase ก่อนใช้งาน"**
**แก้ไข:** 
- ตรวจสอบว่าแก้ไข `supabase-config.js` แล้วหรือยัง
- ตรวจสอบว่า URL และ Key ถูกต้อง (ไม่มีช่องว่างหรือตัวอักษรเกิน)

### **ปัญหา: Error "Failed to fetch"**
**แก้ไข:**
- ตรวจสอบว่า Project URL ถูกต้อง
- ตรวจสอบว่ามี Internet
- ตรวจสอบว่า Supabase Project สถานะเป็น "Active"

### **ปัญหา: Error "JWT expired" หรือ "Invalid API key"**
**แก้ไข:**
- คัดลอก anon public key ใหม่จาก Supabase Dashboard
- ตรวจสอบว่าคัดลอกครบทั้งหมด (Key ยาวมาก)

### **ปัญหา: Error "permission denied"**
**แก้ไข:**
- ตรวจสอบว่ารัน SQL Schema แล้ว (มี RLS Policies)
- ลองรัน SQL ใน `supabase-schema.sql` อีกครั้ง

---

## 📊 ตรวจสอบข้อมูลใน Supabase

### **ดูข้อมูลนักเรียน:**
1. ไปที่ **"Table Editor"**
2. เลือก **"students"**
3. จะเห็นรายชื่อนักเรียนทั้งหมด

### **ดูข้อมูลการเช็คชื่อ:**
1. ไปที่ **"Table Editor"**
2. เลือก **"attendance"**
3. จะเห็นประวัติการเช็คชื่อทั้งหมด

### **ลบข้อมูลทดสอบ:**
1. เลือกแถวที่ต้องการลบ
2. คลิกปุ่ม **"Delete"**
3. หรือใช้ปุ่ม "ล้างข้อมูลทั้งหมด" ในหน้าครู

---

## 🎉 เสร็จสิ้น!

ตอนนี้ระบบของคุณ:
- ✅ เก็บข้อมูลใน Cloud (Supabase)
- ✅ หลายเครื่องเห็นข้อมูลเดียวกัน
- ✅ ข้อมูลไม่หาย (เก็บถาวร)
- ✅ Real-time sync
- ✅ ฟรี 100%

---

## 📝 ขั้นตอนถัดไป (Optional)

### **Deploy ขึ้น Vercel:**
1. Push โค้ดขึ้น GitHub
2. ไปที่ https://vercel.com
3. Import GitHub repository
4. Deploy (1 คลิก)
5. ได้ URL: https://your-project.vercel.app

### **ตั้งค่า Custom Domain:**
1. ซื้อ Domain (เช่น attendance.school.ac.th)
2. ใน Vercel: Settings → Domains
3. เพิ่ม Domain
4. ตั้งค่า DNS ตามที่ Vercel บอก

---

## 💡 Tips

- Export ข้อมูลเป็น CSV เป็นประจำ (สำรอง)
- ตรวจสอบ Supabase Dashboard เป็นประจำ
- ใช้ Table Editor เพื่อแก้ไขข้อมูลโดยตรง
- ดู Logs ใน Supabase เพื่อ Debug

---

## 📞 ต้องการความช่วยเหลือ?

- Supabase Docs: https://supabase.com/docs
- Supabase Discord: https://discord.supabase.com
